<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AliAIAppGenerator"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Text to Image Activity -->
        <activity
            android:name=".activities.TextToImageActivity"
            android:exported="false" />

        <!-- Text to Video Activity -->
        <activity
            android:name=".activities.TextToVideoActivity"
            android:exported="false" />

        <!-- Image Editor Activity -->
        <activity
            android:name=".activities.ImageEditorActivity"
            android:exported="false" />

        <!-- Video Editor Activity -->
        <activity
            android:name=".activities.VideoEditorActivity"
            android:exported="false" />

        <!-- Gallery Activity -->
        <activity
            android:name=".activities.GalleryActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

    </application>

</manifest>